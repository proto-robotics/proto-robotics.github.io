import { b, div, tag } from 'ellipsi'

export default (toolbox, vocab) => {
    const SomeContent = div(
        'placeholder',
        'placeholder',
        b('bold text'),
    )

    return [SomeContent]
}
